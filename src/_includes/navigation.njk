{% if navigation.items %}
<div class="container">
  <nav class="nav">
    <ul>
      {% for item in navigation.items %}
        <li>
          <a href="{{ item.url }}">{{ item.text }}</a>
        </li>
      {% endfor %}
    </ul>
  </nav>
</div>
{% endif %}